<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:Noobsenger.Mobile.Views"
       Title="Noobsenger Mobile"
       IsVisible="False"
       x:Class="Noobsenger.Mobile.AppShell">

    <!--
        The overall app visual hierarchy is defined here, along with navigation.
    
        https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/
    -->
    <!--
        If you would like to navigate to this content you can do so by calling
        await Shell.Current.GoToAsync("//LoginPage");
    -->
        <ShellContent Route="WelcomePage" ContentTemplate="{DataTemplate local:WelcomePage}" />
        <ShellContent Route="HostPage" ContentTemplate="{DataTemplate local:HostPage}" />
        <ShellContent Route="JoinPage" ContentTemplate="{DataTemplate local:JoinPage}" />
        <ShellContent Route="LoginPage" ContentTemplate="{DataTemplate local:LoginPage}" />
        <ShellContent Route="ChatPage" ContentTemplate="{DataTemplate local:ChatPage}" />
</Shell>
